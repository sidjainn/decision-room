services:
  - type: web
    name: ansel-decision-room
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build:public
    startCommand: npm start
    healthCheckPath: /api/smallest/status
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: SMALLEST_API_KEY
        sync: false
      - key: REASONING_PROVIDER
        value: openai
      - key: OPENAI_API_KEY
        sync: false
